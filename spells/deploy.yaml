version: 4

apps:
  $00: n/${app_id}/${app_vk}

public_inputs:
  $00: deploy

private_inputs:

ins:

outs:
  - address: ${next_nft_address}
    charms:
      $00:
        current_cosigners: ${current_cosigners}
        current_threshold: ${current_threshold}
